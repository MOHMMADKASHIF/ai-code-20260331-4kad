const add = (num1, num2) => {
  return num1 + num2;
};

console.log(add(20, 10));


The only change made was adding a curly bracket `{}` to the function declaration to fix the missing semicolon at the end of the function declaration.